/** Templates live in `@/core/templates` — this feature re-exports for UI. */
export {
  TEMPLATE_CATALOGUE,
  getTemplateById,
  getTemplateSwatches,
  listTemplatesByTier,
} from "@/core/templates";
